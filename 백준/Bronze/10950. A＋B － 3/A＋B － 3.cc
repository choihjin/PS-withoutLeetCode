#include <bits/stdc++.h>
using namespace std;

int n, a, b;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;
    while(n--) {
        cin >> a >> b;
        cout << a + b << "\n";
    }
}